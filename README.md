### RoadNote App Installation Guide

Install Global packages
 * `npm install -g bower`
 * `npm install -g http-server`


Install local packages [Optional]
 * `npm install`


Run server via [SH]
 * `sh serve.sh`

Run server via [http-server command]
 * `http-server ./`
